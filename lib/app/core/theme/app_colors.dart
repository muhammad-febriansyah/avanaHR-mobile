import 'package:flutter/material.dart';

/// AvanaHR brand palette (light mode only). Mirrors the web design tokens.
class AppColors {
  static const primary = Color(0xFF2F54C9); // royal blue
  static const navy = Color(0xFF0E1A3A); // deep navy — strong headings
  static const accent = Color(0xFF6E9BE6); // sky
  static const background = Color(0xFFFFFFFF);
  static const muted = Color(0xFFF4F6FB);
  static const border = Color(0xFFE5E9F2);
  static const destructive = Color(0xFFDC2626);
  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFD97706);

  static const textPrimary = Color(0xFF0E1A3A);
  static const textMuted = Color(0xFF64748B);
}
